/** دانش درسی محلی — متن‌ها با املای درست فارسی */

import { wrapAnswer } from '../../services/answerFormat.js';
import { EXTRA_KNOWLEDGE } from './moreCurriculum.js';

function A(q, body) {
  return wrapAnswer(q, body);
}

export const KNOWLEDGE_ENTRIES = [
  // —— علوم / زیست (خاص‌ترها اول) ——
  {
    test: /فتوسنتز|photosynthesis/i,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**فتوسنتز** فرآیندی است که گیاهان با آن غذا می‌سازند.

**مواد لازم:** نور خورشید، آب، دی‌اکسید کربن (CO₂)
**محصولات:** قند (گلوکز) و اکسیژن (O₂)

**فرمول:** ۶CO₂ + ۶H₂O + نور → C₆H₁₂O₆ + ۶O₂

**محل انجام:** بیشتر در برگ‌ها، داخل کلروپلاست (سبزینه)`
      ),
  },
  {
    test: /تنفس\s*سلولی|cellular respiration/i,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**تنفس سلولی** یعنی شکستن قند برای آزاد کردن انرژی در سلول.

**فرمول ساده:** C₆H₁₂O₆ + ۶O₂ → ۶CO₂ + ۶H₂O + انرژی (ATP)

این فرآیند عکس فتوسنتز است و در **میتوکندری** انجام می‌شود.`
      ),
  },
  {
    test: /کلروپلاست|سبزینه/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**کلروپلاست (سبزینه)** اندامک سلول گیاهی است که فتوسنتز در آن انجام می‌شود و رنگ سبز دارد.`
      ),
  },
  {
    test: /میتوکندری/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**میتوکندری** اندامک سلول است که انرژی (ATP) تولید می‌کند و محل تنفس سلولی است.`
      ),
  },
  {
    test: /DNA|دی\s*ان\s*ا[یي]|ژن\b|وراثت|ژنتیک/i,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**DNA** مادهٔ وراثتی داخل هستهٔ سلول است که اطلاعات ساخت موجود زنده را دارد.

**ژن** بخشی از DNA است که یک ویژگی خاص را کنترل می‌کند.`
      ),
  },
  {
    test: /قلب|گردش\s*خون|سرخرگ|سیاهرگ/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**قلب** خون را به بدن پمپ می‌کند.
**سرخرگ** خون را از قلب به بدن می‌برد.
**سیاهرگ** خون را از بدن به قلب برمی‌گرداند.`
      ),
  },
  {
    test: /سلول|یاخته/,
    broad: true,
    answer: (q) =>
      A(
        q,
        `**سلول** کوچک‌ترین واحد زندهٔ سازندهٔ موجودات است.

- **سلول گیاهی:** دیوارهٔ سلولی و کلروپلاست دارد.
- **سلول جانوری:** دیوارهٔ سلولی و کلروپلاست ندارد.

بخش‌های مهم: هسته، سیتوپلاسم، غشا
ترتیب: سلول ← بافت ← اندام ← دستگاه ← موجود زنده`
      ),
  },

  // —— فیزیک ——
  {
    test: /قانون\s*اول\s*نیوتن|اینرسی|لختی/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**قانون اول نیوتن (لختی):** اگر نیروی خالص صفر باشد، جسم حالت سکون یا حرکت یکنواخت روی خط راست را حفظ می‌کند.`
      ),
  },
  {
    test: /قانون\s*دوم\s*نیوتن|F\s*=\s*m\s*a|اف\s*=\s*ام\s*آ/i,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**قانون دوم نیوتن:** F = m × a

نیرو (نیوتن) = جرم (کیلوگرم) × شتاب (متر بر مجذور ثانیه)`
      ),
  },
  {
    test: /قانون\s*سوم\s*نیوتن|کنش\s*و\s*واکنش/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**قانون سوم نیوتن:** برای هر کنش، واکنشی برابر و در خلاف جهت آن وجود دارد.`
      ),
  },
  {
    test: /قانون\s*اهم|ولتاژ|مقاومت\s*الکتریکی|آمپر|مدار\s*الکتریکی/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**قانون اهم:** V = I × R

ولتاژ (ولت) = جریان (آمپر) × مقاومت (اهم)`
      ),
  },
  {
    test: /سرعت\s*متوسط|شتاب\b/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**سرعت متوسط** = جابه‌جایی ÷ زمان
**شتاب** = تغییر سرعت ÷ زمان

واحد سرعت: متر بر ثانیه
واحد شتاب: متر بر مجذور ثانیه`
      ),
  },
  {
    test: /پایستگی\s*انرژی|کار\s*و\s*انرژی|انرژی/,
    broad: true,
    answer: (q) =>
      A(
        q,
        `**انرژی** توانایی انجام کار است. واحد آن ژول (J) است.

**انواع رایج:** جنبشی، پتانسیل، گرمایی، شیمیایی، نوری، الکتریکی

**پایستگی انرژی:** انرژی از بین نمی‌رود؛ فقط از شکلی به شکل دیگر تبدیل می‌شود.`
      ),
  },
  {
    test: /\bنیرو\b|گرانش|وزن\b/,
    broad: true,
    answer: (q) =>
      A(
        q,
        `**نیرو** عاملی است که می‌تواند حرکت جسم را تغییر دهد (شروع، توقف یا تغییر جهت).

**واحد نیرو:** نیوتن (N)
**وزن:** نیروی گرانش زمین روی جسم (W = m × g)`
      ),
  },

  // —— شیمی ——
  {
    test: /جدول\s*تناوبی|عدد\s*اتمی|پروتون|نوترون/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**عدد اتمی** = تعداد پروتون‌ها
**جرم تقریبی اتم** ≈ تعداد پروتون + نوترون

در اتم خنثی، تعداد الکترون با تعداد پروتون برابر است.`
      ),
  },
  {
    test: /اسید|باز\b|قلیا|pH|پی\s*اچ/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**اسید:** معمولاً طعم ترش دارد و pH آن کمتر از ۷ است.
**باز (قلیا):** معمولاً حس لغزنده دارد و pH آن بیشتر از ۷ است.
**خنثی:** pH برابر ۷ (مثل آب خالص)`
      ),
  },
  {
    test: /واکنش\s*شیمیایی|پایستگی\s*جرم/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `در **واکنش شیمیایی** مادهٔ جدید ساخته می‌شود.

**قانون پایستگی جرم:** اتم‌ها از بین نمی‌روند؛ فقط جابه‌جا و دوباره مرتب می‌شوند.`
      ),
  },
  {
    test: /مخلوط|محلول|حلال|حل\s*شونده|تقطیر|فیلتراسیون/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**محلول** = حلال + حل‌شونده (مخلوط همگن)

روش‌های رایج جداسازی: فیلتراسیون، تبخیر، تقطیر`
      ),
  },
  {
    test: /اتم|مولکول/,
    broad: true,
    answer: (q) =>
      A(
        q,
        `**اتم** کوچک‌ترین واحد یک عنصر است (شامل پروتون، نوترون و الکترون).

**مولکول** وقتی ساخته می‌شود که دو یا چند اتم به هم پیوند بخورند.
مثال: آب = H₂O`
      ),
  },

  // —— ریاضی ——
  {
    test: /فیثاغورث|فیثاغورس/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `در مثلث قائم‌الزاویه: a² + b² = c²

(c وتر است؛ ضلع روبه‌روی زاویهٔ ۹۰ درجه)`
      ),
  },
  {
    test: /مساحت\s*مستطیل|محیط\s*مستطیل/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**مساحت مستطیل** = طول × عرض
**محیط مستطیل** = ۲ × (طول + عرض)`
      ),
  },
  {
    test: /مساحت\s*مربع|محیط\s*مربع/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**مساحت مربع** = ضلع × ضلع
**محیط مربع** = ۴ × ضلع`
      ),
  },
  {
    test: /مساحت\s*مثلث/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**مساحت مثلث** = (قاعده × ارتفاع) ÷ ۲`
      ),
  },
  {
    test: /مساحت\s*دایره|محیط\s*دایره/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `اگر r شعاع باشد:
**مساحت دایره** = πr²
**محیط دایره** = ۲πr
**قطر** = ۲r`
      ),
  },
  {
    test: /معادله\s*درجه\s*دوم|دلتا|Δ\s*=/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `برای معادلهٔ ax² + bx + c = ۰:

**دلتا:** Δ = b² − ۴ac
**ریشه‌ها:** x = (−b ± √Δ) / (۲a)

اگر Δ > ۰ دو ریشه، Δ = ۰ یک ریشه، Δ < ۰ ریشهٔ حقیقی ندارد.`
      ),
  },
  {
    test: /کسر|صورت\s*و\s*مخرج|هم\s*مخرج/,
    broad: true,
    answer: (q) =>
      A(
        q,
        `در کسر، **صورت** بالا و **مخرج** پایین است.

برای جمع یا تفریق کسرها: اول مخرج‌ها را هم‌مخرج کنید، بعد صورت‌ها را جمع یا تفریق کنید.`
      ),
  },

  // —— فارسی ——
  {
    test: /فاعل|نهاد/,
    broad: true,
    answer: (q) =>
      A(
        q,
        `**فاعل (نهاد)** کسی یا چیزی است که کار را انجام می‌دهد.

مثال: «علی کتاب خواند» ← فاعل: علی`
      ),
  },
  {
    test: /مفعول/,
    broad: true,
    answer: (q) =>
      A(
        q,
        `**مفعول** کسی یا چیزی است که کار روی آن انجام می‌شود (معمولاً با «را»).

مثال: «علی کتاب را خواند» ← مفعول: کتاب`
      ),
  },
  {
    test: /مسند/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**مسند** ویژگی یا حالتی است که به نهاد نسبت داده می‌شود.

مثال: «هوا سرد است» ← مسند: سرد`
      ),
  },
  {
    test: /آرایه|تشبیه|استعاره|کنایه|جناس/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**تشبیه:** مانند کردن چیزی به چیز دیگر با واژه‌هایی مثل «مثل»، «مانند»، «چون»
**استعاره:** تشبیه فشرده بدون ادات تشبیه
**کنایه:** گفتن چیزی و ارادهٔ معنای دیگر
**جناس:** شباهت آوایی واژه‌ها با معنای متفاوت`
      ),
  },

  // —— مطالعات ——
  {
    test: /نقشه|عرض\s*جغرافیایی|طول\s*جغرافیایی/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**نقشه** تصویر کوچک‌شدهٔ سطح زمین است.

مختصات جغرافیایی شامل **عرض** (شمال/جنوب) و **طول** (شرق/غرب) است.`
      ),
  },
  {
    test: /هخامنشی|کوروش|داریوش/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `سلسلهٔ هخامنشی را **کوروش بزرگ** بنیان گذاشت. **داریوش اول** تشکیلات اداری و راه شاهی را گسترش داد.`
      ),
  },

  // —— انگلیسی ——
  {
    test: /\bto be\b|am\s*,?\s*is\s*,?\s*are|صرف\s*فعل\s*to be/i,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**فعل to be:**
I am / you are / he-she-it is
we-you-they are

گذشته: was / were`
      ),
  },
  {
    test: /present simple|حال\s*ساده/i,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**Present Simple** برای عادت و حقیقت کلی است.

I/you/we/they + verb
he/she/it + verb + s

منفی: don't / doesn't`
      ),
  },

  // —— عربی ——
  {
    test: /فعل\s*ماضی/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**فعل ماضی** کار گذشته را نشان می‌دهد.

مثال از ریشهٔ «کتب»: کَتَبَ، کَتَبَا، کَتَبُوا، کَتَبَتْ و …`
      ),
  },
  {
    test: /فعل\s*مضارع/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `**فعل مضارع** کار حال یا آینده را نشان می‌دهد.

با یکی از حروف أ، ن، ی، ت شروع می‌شود؛ مثل: أَکْتُبُ، نَکْتُبُ، یَکْتُبُ، تَکْتُبُ`
      ),
  },

  // —— قرآن ——
  {
    test: /سوره\s*حمد|فاتحه/,
    specific: true,
    answer: (q) =>
      A(
        q,
        `سورهٔ **حمد** (فاتحه) اولین سورهٔ قرآن است و در هر نماز خوانده می‌شود.`
      ),
  },
];

function isDefinitionLike(text) {
  const q = text.trim();
  if (q.length <= 70) return true;
  return /چیست|یعنی|تعریف|توضیح|شرح\s*بده|چطور|چگونه|\?|؟/.test(q);
}

const ALL_KNOWLEDGE = [...KNOWLEDGE_ENTRIES, ...EXTRA_KNOWLEDGE];

export function matchKnowledge(text) {
  const q = (text || '').trim();
  if (!q) return null;

  let best = null;
  let bestScore = -1;

  for (const item of ALL_KNOWLEDGE) {
    const matched = q.match(item.test);
    if (!matched) continue;
    if (item.broad && !isDefinitionLike(q)) continue;

    let score = (matched[0] || '').length;
    if (item.specific) score += 40;
    if (item.broad) score += 5;
    if (matched.index !== undefined && matched.index < 12) score += 10;

    if (score > bestScore) {
      bestScore = score;
      best = item;
    }
  }

  return best ? best.answer(q) : null;
}
